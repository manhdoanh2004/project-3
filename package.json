{
  "name": "project_3",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile-pug": "pug -w ./template -o ./ -P",
    "compile-sass": "sass --watch assets/scss:assets/css",
    "start": "npm-run-all --parallel compile-pug compile-sass"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
